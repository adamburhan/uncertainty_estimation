# SemiStaticSim stereo dataset config.
# Camera: 640x640, 90deg FoV, fx=fy=320, cx=cy=320
# Stereo configs: {horizontal,vertical}_{5,10,20,50,100}cm

name: semistaticsim
root: /Volumes/T9/datasets/sss/generated_data/semistaticsim
split_dir: test                   # subdirectory under root: "train" or "test"
scenes_train: [1]
scenes_val:   [1]
stereo_config: horizontal_20cm    # {horizontal,vertical}_{5,10,20,50,100}cm
seed: 0
run_id: 0
depth_source: gt
max_depth: 200.0
